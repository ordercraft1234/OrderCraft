import { Pending, Screen } from '../components/Screen.tsx'

/** Artboard 2. The ribbon field, the row of counts, and the triple list — T023. */
export function Compare() {
  return (
    <Screen title="Comparison" subtitle="No slot loaded">
      <Pending task="T023">
        Two orderings of one recorded block and the ribbon field between them, marked moved /
        delayed / dropped / deferred. The note about the modelled arrival scale that has to sit
        under it is T024.
      </Pending>
    </Screen>
  )
}
